# Build history

- `failed-theos-workflow.yml`: the Theos GitHub Actions workflow that was tried and failed before
  the move to the rootless build. It is kept as a reference so the same mistake is not repeated;
  the working pipeline is `.github/workflows/build-rootless.yml`.
